<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.0.0-alpha1/css/bootstrap.min.css" integrity="sha384-r4NyP46KrjDleawBgD5tp8Y7UzmLA05oM1iAEQ17CSuDqnUK2+k9luXQOfXJCJ4I" crossorigin="anonymous">
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/regular.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/solid.min.css"> -->
    
    <title>app dashboard</title>
</head>
<body>
    <header id="header" class="d-flex flex-row justify-content-between bg-main">
        <div class="p-3">
            <a href="/" class="text-dark text-decoration-none"><h2 class="mb-0 font-weight-bold">我们的家</h2></a>
        </div>
        <% if(user) { %>
            <div class="p-3 d-flex flex-row">
                <a href="#" class="text-dark px-2"><i class="fa fa-search fa-2x"></i></a>
                <a href="/profile/<%= user.usersymbol %>" class="px-2"><img class="img-fluid rounded-circle avatar-header" src="<%= user.avatar %>" alt=""></a>
                <a href="/create" class="btn btn-light bg-light text-dark mx-2 px-4">创建</a> 
                <form action="/logout" method="POST" class="mx-2">
                    <button type="submit" class="btn btn-light bg-light text-dark px-4">登出</button>
                </form>
            </div>
            <% } else { %>
                <div class="p-3">
                    <form action="/login" method="POST" class="">
                        <div class="row align-items-center">
                            <div class="col-md mr-0 pr-md-0 mb-3 mb-md-0">
                                <input type="text" name="username" id="username" class="form-control form-control-sm" placeholder="用户名">
                            </div>
                            <div class="col-md mr-0 pr-md-0 mb-3 mb-md-0">
                                <input type="password" name="password" id="password" class="form-control form-control-sm" placeholder="密码">
                            </div>
                            <div class="col-md-auto">
                                <button type="submit" class="btn btn-light btn-sm">登录</button>
                            </div>
                        </div>    
                    </form>
            <% } %>    
        </header>
        
        